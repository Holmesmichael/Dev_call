%h1= @event.title
%p= @event.description
%p
Event established by
= @event.user.email

= link_to "Back", root_path
= link_to "Edit", edit_event_path
= link_to "Delete", event_path, method: :delete, data: { confirm: "Are you sure?" }

